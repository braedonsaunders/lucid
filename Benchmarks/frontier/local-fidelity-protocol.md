# Local fidelity constraints for the input-conditioned controller

Frozen September 5, 2026 before this experiment. The completed dynamic controller improves development LPIPS/DISTS by 5.70%/6.24% but loses too much true detail. A fresh matched diagnostic proves that the original folded reconstruction passes the same per-source detail limits. This experiment changes the training objective, retaining the 7,408-parameter controller informed by September 2026 SPARK. It is neither a new backbone nor a claim of research novelty.

Add two local constraints against the **known training reference**: RGB squared error and signed RGB Sobel squared error should not exceed the corresponding cached shipping error in each spatial tile. Average channels within each 8×8 tile, including remainder tiles. Normalize excess by the reference's local energy plus shipping's local error plus 0.0001; detach reference and baseline. Average positive excess within each domain. This prevents improvement elsewhere from directly canceling a locally worse tile. Signed gradients penalize displaced structure even if its texture energy is unchanged. The objective does not compute LPIPS, DISTS or the evaluation's Pearson fine correlation.

Use initial nonnegative multipliers [1, 1], an additional quadratic penalty with rho 10, and multiplier ascent `lambda += 0.05 * detached_violation` after each optimizer step. These constants are fixed before training. This is an adaptive penalty experiment, not a proof of constraint satisfaction or generalization. Record both domain violations and multipliers every 200 steps. Compare aligned crops and augmentations explicitly; the baseline comes from the full-context shipping cache, not another differently padded inference.

Everything else remains the completed dynamic controller recipe: frozen area-folded reconstruction, exact channel masks/RMS, seed 20260914, 8,000 steps, batch four, crop 96, AdamW 0.0002 cosine schedule, the existing 50/50 target, reference Sobel/FFT supervision and the released PixRestore DINO adversary at weight 0.005. Run a new unconstrained two-step smoke to reproduce the previous smoke's actual final controller hash, then a constrained smoke with matching initial identities. Evaluate only the final unblended constrained checkpoint against shipping and the completed unconstrained dynamic arm. No new flicker loss, model branch, inference operation, or shipping asset change is introduced.

Use the unchanged 48 full-frame development pairs and 96 bank-validation pairs, with identical scorer/checkpoint/control checks. Require the existing ≥3% LPIPS/DISTS gains, ≤2% per-source perceptual regressions and ≤0.01 fine-correlation losses. If both development screens pass, freeze the model before fresh source-disjoint quality testing and native delivered evaluation. No parameter or penalty sweep follows a failure. Retain all negative results.

## Control preflight stopped; deterministic pairing required

The new unconstrained two-step smoke matches all ten initial identities but not the prior final controller hash. Actual tensor differences are small (maximum 9.01e-7; reconstruction tensors unchanged), yet the predeclared exact check failed and stopped before constrained training. This does not establish a code regression or prove nondeterminism as the cause. The observed discrepancy is preserved in `local-fidelity-initial-differences.json`.

The revised run enables strict PyTorch deterministic algorithms, deterministic cuDNN with benchmark disabled, and `CUBLAS_WORKSPACE_CONFIG=:4096:8` before process startup. It requires two independent deterministic unconstrained smokes to reproduce the exact final state, followed by a constrained smoke sharing the new initial identities. It then trains **fresh unconstrained and constrained 8,000-step arms** under the same deterministic execution. The earlier nondeterministic full controller is retained as historical evidence, not substituted for this matched control. Unsupported deterministic operations must fail rather than silently fall back to nondeterministic execution. The objective, data, seed, coefficients, endpoint and quality gates remain unchanged.

Strict execution exposed an unsupported CUDA antialiased bicubic backward in DINO preprocessing; the r2 smoke and dependent evaluation stopped without any full training. The r3 preflight replaces only this training resize with separable matrix products whose coefficients are obtained from CPU PyTorch impulse responses. Images and gradients remain on the GPU; FP32 matrix arithmetic runs outside BF16 autocast with TF32 disabled. CPU forward and backward equivalence tests pass over upsample/downsample/boundary cases. A separate CUDA preflight requires <1e-6 forward error, <3e-6 gradient error, and exact repeated outputs/gradients before any training smoke. The new paired full runs still require exact repeated smoke state and unchanged training/evaluation gates.

The first CUDA equivalence check rejected CPU-generated resampling coefficients: backend coordinate rounding produced 1.16e-6 output and 4.11e-6 gradient differences, beyond the declared limits. The corrected implementation derives constant coefficients using the **same backend's forward** impulse responses; no resampler backward is needed. A separate CUDA probe then passes with maximum forward error 1.79e-7 and backward error 1.20e-6, with exact repeated outputs and gradients. At the actual 176→224 training resize, forward error is zero. The fresh r4 run repeats this check with canonical code hashing before training; neither tolerance was relaxed. Earlier failed directories remain intact.

## Durable execution and verification

The accepted implementation is committed with strict deterministic execution. Ten focused CPU tests pass across activation identity/frozen weights, local fidelity gradients and DINO supervision, plus the separate bicubic forward/backward equivalence test. The canonical CUDA resize receipt is `local-fidelity-r4-resize-cuda-preflight.json`; its code hash matches the local implementation.

Remote experiment directory: `C:\lucid\local-fidelity-20260905-r4`. Task `LucidJob-local-fidelity-train-r4-20260905` checks resize equivalence, exact repeated unconstrained smokes, the matched constrained smoke, then runs both 8,000-step arms. `LucidJob-local-fidelity-eval-r4-20260905` waits for successful completion and scores the final checkpoints on both frozen sets. It refuses competing Python jobs and takes an exclusive experiment lock. It stops on failed training or controls; completing the task does not mean the quality gates passed. Both tasks survive an initiating SSH disconnect.

Fetch `Benchmarks/frontier/local-fidelity-{development,bank}.json` and corresponding `-gate.json` files, both final checkpoints and experiment/completion receipts after completion. Re-run `local-fidelity-score.py` locally against downloaded checkpoint hashes and independently inspect the frozen reconstruction tensors before considering native evaluation. No remote result automatically changes bundled models or authorizes promotion.

## Completed matched experiment: neither arm promoted

Both strict deterministic 8,000-step arms and both automatic evaluations complete successfully. Downloaded tensors independently reproduce the completion hashes; the frozen anchor is unchanged, and all ten initial provenance fields match. Training takes 9.18 minutes unconstrained and 10.39 minutes constrained on the RTX 4080. Local gate recomputation reproduces both shipping/interpolation controls exactly.

| Set | Unconstrained LPIPS / DISTS improvement | Constrained LPIPS / DISTS improvement | Detail result |
|---|---|---|---|
| 48 development pairs | 5.71% / 6.24% | 1.00% / −0.10% | Unconstrained fails all three; constrained passes all |
| 96 bank-validation pairs | 8.86% / 4.42% | 1.16% / 0.44% | Unconstrained fails both REDS sources; constrained passes all |

The local penalty protects detail on both repeated development sets, but removes most perceptual improvement. Neither arm meets the unchanged joint gate. No coefficient/checkpoint sweep follows, and no bundled model is replaced. The completed receipts, raw scores, gates and independent verifier remain beside this protocol; final checkpoints remain in `.build/local-fidelity-results` and the remote r4 experiment directory.

## Training-context diagnostic

After rejecting the matched endpoints, a read-only RTX 4080 probe compares the same frozen area-folded reconstruction on full training patches versus even-aligned center crops of 96 pixels. It uses the lexicographically first training sequence and first frame from each of 26 source identities; no validation footage, optimization or teacher selection. The exact script/data/checkpoint hashes and all rows are in `training-context-probe.json`.

With the training objective's eight-output-pixel exclusion, source-balanced absolute context difference is 0.0575 RGB levels, about 1.05% of the corresponding reference error on average (per-source ratios averaged). At 32 pixels it falls to 0.00228 levels, and at 64 pixels to 0.00000331 levels. Individual edge-adjacent peaks are larger, but this limited probe does not support crop context as the dominant explanation of the failed perceptual/detail tradeoff. It is not evidence that all random crops or the learned pooled controller are context-invariant. No new training run follows this diagnostic.
