# Local-conditioning mechanism and cost preflight

Specified September 5, 2026, before this prototype's measurements. The completed training-only probe found that the global controller changes interior output when crop context changes, even after convolution-padding effects vanish. Test a fixed local alternative within the existing SPARK-informed sparse modulation experiment. This is not a novelty claim or a demonstrated quality improvement.

Replace global feature mean/absolute-mean pooling with a nine-by-nine feature neighborhood at stride one. Apply the same 7,408-parameter controller at each feature position. Keep the six blocks, four selected channels, gain/shift bounds, normalization, frozen reconstruction, and parameter initialization. Nine is fixed before results; no neighborhood-size search. Count-excluding-padding average pooling is used at frame edges. No temporal objective is added.

First require zero-control identity (including BF16), real controller gradients with immutable reconstruction, checkpoint roundtrip, and nonzero crop-interior consistency. On the same 26 training-only source samples, reinterpret the fixed constrained R4 controller in this local architecture solely as a mechanism probe. Compare full256 input with its aligned center160 crop, excluding an 88-output-pixel border. Require each source's interior mean difference <=0.001 RGB levels and maximum <=0.05 levels. Retain the global-control differences for comparison. These outputs are not a trained local model and are not scored for quality selection.

Then export the nonzero local controller and interleave the unchanged folded reconstruction on this Mac at 360p and 720p, using the existing fixed conversion policy and RGB correctness limits (maximum3 / mean0.6 levels). Require 360p model mean<=8ms/p95<=10ms and 720p mean<=25ms/p95<=30ms before another training run. These budgets reserve time for the remaining native pipeline; passing them does not prove delivered cadence or enable 720p. No native profiling overlaps the browser timing experiment.

Only a passing mechanism/cost preflight proceeds to a fresh deterministic local-controller training endpoint. Retain the constrained R4 objective, data, seed and endpoint; reproduce initial identities and repeated CUDA smokes before full training. The existing perceptual/detail gates remain unchanged. Shipping weights and all prior rejected results remain preserved.


## Initial preflight and exact implementation optimization

Three local-controller tests and four existing global-controller regressions pass. The RTX4080 training-only probe completes all 26 sources. At the declared crop and interior margin, global conditioning averages 0.05936 RGB levels of context difference; local9 is exactly zero on all sources. The first supervisor misread a completed child process's exit code after `WaitForExit`; the corrected `Start-Process -Wait -PassThru` job completes successfully. No model training occurred, and the first result is preserved remotely.

The dense-mask Core ML implementation passes the fixed RGB conversion limits. Its 360p mean/p95 is 6.972/7.195ms and its 720p mean/p95 is 26.123/26.430ms. The latter misses the 25ms mean gate; no training admission follows. Because the masks are fixed one-hot selectors, replace their dense matrix products with equivalent indexed channel selection and remeasure. This changes implementation, not the fixed neighborhood, coefficients, weights, bounds or quality gates. Preserve the dense source and timing evidence.

The direct-selection implementation exactly matches dense output on the CPU FP32/BF16 equivalence probes, but Core ML is slower at 720p (27.289ms mean versus 26.123ms dense). Both miss the unchanged 25ms gate. Retain the simpler dense implementation and preserve both source versions and all timings. No training run is launched from these failed cost preflights.

## Separate coarse-grid prototype

After both full-grid implementations miss the unchanged cost gate, test one explicitly different architecture: retain the nine-feature neighborhood but evaluate its MLP at stride two, then bilinearly interpolate the 48 control maps with align_corners=False. This reduces MLP positions fourfold and changes the function; it is not claimed equivalent to dense local conditioning. Keep channel selections, parameter count and bounds. Test aligned center160 crops with a 96-output-pixel margin, increased before execution to cover the larger interpolation footprint. This check establishes locality only for the tested grid alignment; arbitrary sub-grid shifts and temporal behavior remain unproven. Keep the same per-source error limits and 360p/720p cost budgets. Do not select neighborhood size or interpolation from quality results. New CUDA smokes must verify deterministic training behavior, including interpolation backward, before any full training admission.

The coarse prototype passes its declared CUDA locality check on all 26 training identities: mean interior difference is 0.00000509 RGB levels (global control 0.05947 at the same 96-pixel margin). Six CPU tests pass, and two independent three-step synthetic CUDA/BF16 optimizer runs reproduce exact controller hashes with immutable anchors. These are gradient/determinism unit checks, not data training. Three additional admission tests reject absent/substituted graphs, missing 720p measurements, excessive latency and nonfinite or incorrect output. Full training remains programmatically gated on matching native source hashes and the unchanged cost limits.

The first coarse native run passes numeric correctness but misses both cost gates. Its unchanged folded 720p control also slows from about21.1ms to31.6ms; shipping4x slows from35.7ms to55.4ms. Post-run inspection shows other active host work, AC power and no recorded thermal warning. Retain this run. Before repeating the unchanged candidate, require two baseline-only probes at least30seconds apart with mean<=23.25ms and p95<=26ms, within the earlier control regime. Record every probe. No other host process is stopped and no candidate is changed or admitted on the slow run.


Two standalone control probes 46.6seconds apart recover to20.76/20.86ms, meeting the declared recovery criterion. Repeating the full export/Python comparison still slows its controls (720p folded27.97ms and shipping46.20ms). These runs remain failed cost observations; no graph, neighborhood, or cost limit changes.

To distinguish deployment runtime from the export/Python process, measure the same saved model packages directly with Swift CoreML: initialized deterministic BGRA inputs, CPU+GPU, image-buffer access, ten warmups, 120 alternating-order samples, and explicit autorelease scopes. All 18 package-file hashes are verified. The linked preflight retains the original numeric conversion checks and both Python timings, and identifies this different timing backend explicitly. Coarse model means/p95 are6.281/6.894ms at360p and23.234/23.453ms at720p; the unchanged folded control is5.152/19.718ms mean. This direct native measurement passes the unchanged8/10ms and25/30ms budgets, without Python/PIL or export work. It is still a model-stage preflight, not native playback or quality admission.

Task `LucidJob-coarse-controller-r1-20260905` is launched in a fresh remote directory. It runs two deterministic, constrained, two-step data smokes; verifies11 initial identities against constrained R4 and exact repeated controller hashes; then trains only the fixed8,000-step endpoint. It automatically evaluates that endpoint on the frozen48-pair development and96-pair bank-validation sets, reproducing shipping/Lanczos controls before applying the unchanged3% perceptual/2% per-source regression/0.01 fine-correlation gates. No checkpoint selection, blending, or shipping replacement is authorized by mere completion. Source, native-admission and job evidence is retained. This is a new quality experiment; the separately verified shipping tensor-output optimization is unchanged.
